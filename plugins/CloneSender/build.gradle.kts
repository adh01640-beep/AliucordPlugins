version = "1.0.0"
description = "-رسالة"

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial plugin release.
        """.trimIndent()
    )
    author("Adham")
    deploy.set(true)
}

version = "1.0.0"
description = "Seamlessly completes available Discord quests in the background and provides an integrated menu to track and claim your earned rewards."

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial plugin release.
        """.trimIndent()
    )
    author("Adham", 1001222848716738570L, hyperlink = true)
    deploy.set(true)
}

version = "1.0.0"
description = "Fixes autocomplete by adding pomelo username support."

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial plugin release.
        """.trimIndent()
    )
    author("Adham", 368398754077868032L, hyperlink = true)
    deploy.set(true)
}

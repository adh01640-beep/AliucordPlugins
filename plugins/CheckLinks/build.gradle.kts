version = "1.0.0"
description = "check links before opening them."

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial plugin release.
        """.trimIndent()
    )
    author("Adham")
    deploy.set(true)
}
